name: site_scope
description: SiteScope — Professional Sun & Wind direction tool for architects.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.6.1
  sensors_plus: ^6.1.1
  geolocator: ^13.0.2
  flutter_compass: ^0.8.1
  http: ^1.3.0
  fl_chart: ^0.70.2
  vector_math: ^2.1.4
  intl: ^0.20.2
  permission_handler: ^11.4.0
  google_fonts: ^6.2.1
  shared_preferences: ^2.5.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  flutter_launcher_icons: ^0.13.1

flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/logo/icon_fg.png" # We'll need to generate this or config it via code
  min_sdk_android: 21
  adaptive_icon_background: "#F4F3EF"
  adaptive_icon_foreground: "assets/logo/icon_fg.png"

flutter:
  uses-material-design: true
